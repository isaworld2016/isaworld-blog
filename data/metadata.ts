import { IMetadata } from "../common/interface/IMetadata";

const metadata: IMetadata = {
  title: "Isaworld Blog",
  description: "천재적인 이자님의 블로그",
  author: "isaworld",
  rowsPerPage: 10
};

export default metadata;